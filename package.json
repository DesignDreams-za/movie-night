{
  "name": "movie-night",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev -w client\" \"npm run dev -w backend\"",
    "dev:client": "npm run dev -w client",
    "dev:backend": "npm run dev -w backend",
    "build": "npm run build -w client && npm run build -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
