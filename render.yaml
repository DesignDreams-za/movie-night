services:
  - type: web
    name: movie-night-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: CLIENT_ORIGIN
        # Comma-separated list of allowed front-end origins.
        value: https://movie-night-doky.netlify.app,https://movie-night-watchparty.netlify.app
