export function PrivacyFooter() {
  return (
    <p className="text-center text-[11px] leading-relaxed text-gray-600">
      🔒 Your movie file and voice call never touch our server — video is loaded locally and voice
      is peer-to-peer. Chat messages are relayed to keep both sides in sync (encrypted in transit,
      never logged or stored). This is a private tool for personal use — you're responsible for
      what you watch and share here; we don't monitor session content.
    </p>
  )
}
